const UserInputText = async (inputs = {}, nodeValue = {}, context) => {
  // console.log('inputs', inputs);
  const result = context.question;
  // console.log('UserInputText', result);
  return result;
}

module.exports = exports = UserInputText;